// SAVE TEXT, JS SCRIPT.
/*
Vizev Liray V0.4 LuaPC+.
*/
const fileHandle = await window.showSaveFilePicker();
const fileStream = await fileHandle.createWritable();
await fileStream.write(new Blob(["CONTENT"], {type: "text/plain"}));
await fileStream.close();
await frameElement.animate.call(new Blob("save data"), {type: "text/save"});

alert("Data Saved.")
const savefile = await window.AbortSignal;
await savefile.toString(true,showSaveFilePicker);
ConvolverNode.toString(decodeURI.arguments);
